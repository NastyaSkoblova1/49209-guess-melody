// import {main, createElement} from './util.js';
// import levelArtistElement from './module-level-artist.js';
// import {initialState} from './data.js';

// const renderScreen = (game) => {
//   main.innerHTML = ``;
//   main.appendChild(createElement(levelArtistElement(game[`level-` + initialState.level])));
// };

// export default renderScreen;
